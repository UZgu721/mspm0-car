# MSPM0G3507 八路数字灰度循迹小车

本工程基于 TI MSPM0G3507、CCS 和 DriverLib，实现双电机编码器闭环巡黑线。灰度模块使用 8 路直接数字输入：白色输出高电平、黑线输出低电平。工程不使用陀螺仪、I2C、ADC、DMA 或 CD4051。

## 灰度输入

| 左→右通道 | GPIO | 白色 | 黑线 | `Read_HuiDu()` 位 |
|---|---|---:|---:|---|
| G0 | PA12 | 高 | 低 | bit0 |
| G1 | PA8 | 高 | 低 | bit1 |
| G2 | PA9 | 高 | 低 | bit2 |
| G3 | PA27 | 高 | 低 | bit3 |
| G4 | PA24 | 高 | 低 | bit4 |
| G5 | PA7 | 高 | 低 | bit5 |
| G6 | PA22 | 高 | 低 | bit6 |
| G7 | PA15 | 高 | 低 | bit7 |

程序把低电平黑线转换为逻辑 `1`。OLED 的灰度字符串按物理左→右显示 G0 至 G7。

## 控制与计时

- PB8 内部上拉、低电平按下；按住超过约 700 ms 后松开，启动巡线并清零计时。
- 编码器速度 PID 和灰度位置 PID 共同控制电机；基础巡线速度为 `375`，半圆弯道增强参数为 `Kp=0.9`、`Kd=0.35`、最大差速 `±800`。
- PA24、PA7、PA22，即 G4 至 G6，同时检测到黑线时，立即停止两侧电机，作为终点标记。
- 启动后，计时器以 10 ms 为单位运行，OLED 显示整数秒。
- 终点标记触发软件停车时，计时在同一个 10 ms 控制周期内立即冻结，不依赖编码器是否曾检测到脉冲。
- 只有至少检测到一次编码器脉冲后，才允许结束判定。
- 左右轮编码器连续 500 ms 都无脉冲时，计时冻结为最后一次检测到轮子运动的时间；这是车辆意外卡住时的后备结束判定。
- 再次长按仅当计时已结束时才会重新开始一轮计时。

## OLED

```text
L:+0000 R:+0000     左右轮最近 10 ms 编码器增量
G:00000000          左→右 G0 至 G7；1=黑线，0=白色
RUN:WAIT            等待长按启动；启动后显示 RUN:ON
T:00000s            行驶时间，单位为秒；停止后保留结果
```

UART1 的 PB6/PB7 接收逻辑仍在后台工作，但不再占用 OLED 第四行。

## 主要引脚

| 功能 | 信号 | 引脚 |
|---|---|---|
| 左电机 PWM | TIMG6 CCP0 | PB2 |
| 右电机 PWM | TIMG6 CCP1 | PB3 |
| 左电机方向 | AIN1 / AIN2 | PA13 / PA14 |
| 右电机方向 | BIN1 / BIN2 | PA17 / PA16 |
| 左编码器 | E1A / E1B | PA25 / PA26 |
| 右编码器 | E2A / E2B | PB20 / PB24 |
| 8 路灰度 | G0 至 G7 | PA12、PA8、PA9、PA27、PA24、PA7、PA22、PA15 |
| OLED | RST / DC / SCL / SDA | PB14 / PB15 / PA28 / PA31 |
| 启动按键 | KEY | PB8 |
| 蓝牙串口 | UART1 TX / RX | PB6 / PB7 |
| 备用串口 | UART2 TX / RX | PB17 / PB16 |
| SWD 下载 | SWDIO / SWCLK | PA19 / PA20 |

## 构建与硬件注意事项

在 CCS 选择 **Debug** 构建，生成 `Debug/hengtuobei2025.out` 和 `.hex`；下载目标配置为 `targetConfigs/MSPM0G3507.ccxml`，使用 J-Link SWD。

灰度模块 DO 输出必须是 **3.3 V 逻辑**。若模块由 5 V 供电且高电平为 5 V，必须先电平转换，不能直接连接 MSPM0 GPIO。
